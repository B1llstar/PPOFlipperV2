# Afk Woodcutting

Chops the tree named in config (`Tree`, `Oak`, `Willow`, ...) on repeat. When the
inventory fills, either drops all logs or banks, depending on `dropLogs`.

Example plugin for the BotStar repo — copy this package as a starting point
for a new script.
